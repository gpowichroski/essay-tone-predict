---
declared-services:
  predictinator:
    label: cloudantNoSQLDB
    plan: Lite
  predictinator-analyzer:
    label: tone_analyzer
    plan: standard
applications:
- disk_quota: 1024M
  host: predictinator-teacher
  name: predictinator-teacher
  path: ./toneAnalysis.war
  instances: 1
  buildpack: https://github.com/cloudfoundry/java-buildpack.git
  memory: 512M
  services:
    - predictinator
    - predictinator-analyzer